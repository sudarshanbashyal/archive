import React from 'react';
import { BrowserRouter, Switch } from 'react-router-dom';

const Home = () => {
    return (
        <div>
            <BrowserRouter>
                <Switch></Switch>
            </BrowserRouter>
        </div>
    );
};

export default Home;
